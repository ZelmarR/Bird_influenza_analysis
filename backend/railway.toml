[build]
builder = "nixpacks"

[deploy]
startCommand = "pip uninstall -y passlib python-jose 2>/dev/null; uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
